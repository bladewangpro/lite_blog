
markdown: kramdown

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag

defaults:
  - scope:
      path: "categories"
    values:
      layout: "category"
  - scope:
      path: ""
    values:
      layout: "default"
  